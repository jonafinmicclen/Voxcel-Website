---
import { Image } from 'astro:assets';
import logo from '@/images/logo.png';

const sections = [
  { id: "about", label: "About Us" },
  { id: "benefits", label:"Why Voxcel"},
  {id:"timeline", label:"Our Journey"},
];
---

<nav class ='w-full h-64 xl:h-32 flex flex-row space-x-10 bg-white shadow-md shadow-indigo-50 px-15 lg:px-10 xl:px-5 '>
        
        <Image src={logo} alt="Voxcel logo" class="w-1/7 h-auto"/>
        
        {sections.map(({ id, label }) => (
        <a href={`#${id}`} class="transform transition-all duration-300 ease-in-out hover:scale-115 text-4xl xl:text-xl font-semibold font-mono text-stone-600 my-auto">{label}</a>
        ))}

    </nav>
</header>   