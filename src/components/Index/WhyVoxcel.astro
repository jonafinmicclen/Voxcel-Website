---
const benefits = [
  {
    title: "Early Detection",
    description: "Detect before hospitalisation",
  },
];
---

<section class="w-full h-32 bg-indigo-300 flex justify-center items-center">
  <p class="text-6xl font-medium font-mono text-stone-600 text-center">
    Risk aversion through Voxcel
  </p>
</section>

<section class="w-full bg-indigo-300 py-12 px-4 ">
  <div class="max-w-5xl mx-auto grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 gap-6">

    {benefits.map((benefit) => (
      <div class="rounded-lg bg-orange-300 p-6 shadow-md" key={benefit.title}>
        <h3 class="text-xl font-mono font-semibold text-stone-700 mb-2">{benefit.title}</h3>
        <p class="font-mono text-stone-600">{benefit.description}</p>
      </div>
    ))}

  </div>
</section>